# Gator CLI

Thisis a basic RSS feed aggregator using postgres and golang.

```
# add a username to the database
gator register <username>
```

```
# adds a feed to the database
gator addfeed <name> <url>
```
